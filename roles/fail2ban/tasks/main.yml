---
- name: Install fail2ban
  apt:
    name:
      - fail2ban
    state: latest

- name: Copy jail.local config
  template:
    src: jail.local.j2
    dest: /etc/fail2ban/jail.local
    owner: root
    group: root
    mode: 0644
  notify: restart fail2ban
